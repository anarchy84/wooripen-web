# 인수인계 문서 — 2026-04-17 세션

## TL;DR
이번 세션에서 두 가지 큰 작업을 했다:
1. **Supabase 프로젝트를 법인 Org로 이전** (완료)
2. **GNB 누락 메뉴 + 패키지/FAQ 페이지 생성** (코드 완료, 시드 SQL 실행 대기)

---

## 1. Supabase Org Transfer ✅ 완료

### 무슨 일?
- `wooripen` 프로젝트를 `anarchy84's Org` (개인) → `Wooripen` Org (법인)으로 이전
- Project Ref (`llnzuczikgvbxxujztao`)는 변경 없음
- 모든 URL, API 키, DB 연결 그대로 유지됨

### 후속 조치 필요
| 항목 | 상태 | 설명 |
|------|------|------|
| Supabase MCP 재인증 | ⏳ 필요 | Cowork/Cursor의 Supabase MCP 토큰이 옛날 Org에 묶여 있음. Wooripen Org 스코프로 재연결 필요 |
| Vercel 법인 Team 생성 | ⏳ 다음주 | 법인카드 발급 후 Vercel Team 생성 → 프로젝트 Transfer |
| Vercel 프로젝트 Transfer | ⏳ 다음주 | Team 생성 후 진행 |

---

## 2. GNB + 패키지허브 + FAQ 페이지 🔄 대웅 액션 필요

### 문제
GNB에 "소상공인 패키지", "회사소개", "FAQ" 메뉴가 없었음.
`/packages` 허브 페이지, `/faq` 공개 페이지도 없었음.

### 생성된 파일 4개

```
supabase/seed_packages_faqs_gnb.sql   ← 시드 데이터 (SQL Editor에서 실행)
app/packages/page.tsx                  ← 패키지 허브 페이지 (SSR)
app/faq/page.tsx                       ← FAQ 공개 페이지 (SSR)
app/api/faqs/route.ts                  ← FAQ 공개 API
```

### 대웅 실행 순서

**Step 1: 시드 SQL 실행**
- Supabase 대시보드 → SQL Editor 열기
- `supabase/seed_packages_faqs_gnb.sql` 내용 복사 → 붙여넣기 → Run
- 결과: 패키지 6개 + FAQ 15개 + GNB 메뉴 3개 추가됨

**Step 2: git add**
```bash
git add app/packages/page.tsx app/faq/page.tsx app/api/faqs/route.ts supabase/seed_packages_faqs_gnb.sql
```

**Step 3: commit**
```bash
git commit -m "feat: 패키지 허브 + FAQ 페이지 + 시드 데이터 + GNB 메뉴 추가"
```

**Step 4: push**
```bash
git push
```

**Step 5: Vercel 빌드 확인**
- Vercel 대시보드에서 빌드 성공 확인

**Step 6: QA 체크**
- [ ] `/packages` 접속 → 패키지 카드 6개 보이는지
- [ ] `/faq` 접속 → 카테고리별 FAQ 아코디언 동작하는지
- [ ] `/about` 접속 → 회사소개 페이지 정상 노출
- [ ] GNB에 "소상공인 패키지", "회사소개", "FAQ" 메뉴 보이는지
- [ ] 모바일 햄버거 메뉴에서도 3개 메뉴 보이는지

---

## 3. 시드 데이터 상세

### 패키지 6개
| slug | 이름 | 업종 | 훅 카피 |
|------|------|------|---------|
| startup | 초기창업자 패키지 | 음식점 | 창업 첫 달, 필수 3종 한 번에 끝 |
| support | 사업자 응원 패키지 | 전체 | 기존 계약, 더 좋은 조건으로 바꾸세요 |
| cafe | 카페 전용 패키지 | 카페 | 스마트 카페 세팅, 우리편이 한 번에 |
| restaurant | 음식점 올인원 패키지 | 음식점 | 음식점 필수템, 한 번에 상담받으세요 |
| beauty | 뷰티샵 패키지 | 미용실 | 뷰티샵 맞춤 솔루션, 우리편에서 |
| rental-bundle | 렌탈 통합 패키지 | 전체 | 렌탈 묶음 할인, 최대 40% 절감 |

→ **샘플이니까 직원들한테 어드민에서 내용 수정하라고 전달**

### FAQ 15개 (카테고리별)
- 일반(general): 3개
- 인터넷(internet): 3개
- 결제단말기(terminal): 2개
- CCTV(cctv): 2개
- 키오스크(torder): 2개
- 렌탈(rental): 2개

→ **마찬가지로 어드민에서 추가/수정 가능**

### GNB 메뉴 3개 추가
- 소상공인 패키지 → `/packages` (sort_order: 9)
- 회사소개 → `/about` (sort_order: 10)
- FAQ → `/faq` (sort_order: 11)

→ **어드민 → GNB 메뉴 관리에서 순서 조정 가능**

---

## 4. 다음 세션에서 이어갈 작업

### 즉시 (이번 작업 후속)
- [ ] 시드 SQL 실행 결과 확인
- [ ] 배포 후 QA
- [ ] GNB 메뉴 순서 최종 조정 (어드민에서)

### 보류 중 (Phase 2 계속)
- [ ] Supabase MCP 재인증 (Wooripen Org)
- [ ] Phase 2-A 마이그레이션 6종 Supabase dev 브랜치 테스트
- [ ] C0 상담 파이프라인 커밋 (대웅 대기 중)
- [ ] ARCHITECTURE.md 초안 작성
- [ ] 도메인 서브도메인 연결 (Cloudflare)

### 다음주
- [ ] Vercel 법인 Team 생성 (법인카드 발급 후)
- [ ] Vercel 프로젝트 Transfer

---

## 5. 기술 참고사항

- TypeScript 타입 체크 통과 (`tsc --noEmit` 성공)
- 패키지 허브: SSR (서버 컴포넌트), Supabase server client 사용
- FAQ 페이지: SSR, `FAQ_CATEGORIES` 타입 재사용, 카테고리별 아이콘/색상 매핑
- FAQ 공개 API: CDN 캐시 120초 설정
- 시드 SQL: `ON CONFLICT` 처리로 중복 실행 안전
- `faqs` 테이블: `CREATE IF NOT EXISTS` 포함 (테이블 없을 경우 자동 생성)
